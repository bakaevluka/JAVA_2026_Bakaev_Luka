Переменная b объявлена внутри внутреннего блока {}. Её область видимости ограничена этим блоком. После закрывающей фигурной скобки переменная b перестает существовать, поэтому в строке int c = a + b; компилятор не может найти переменную b.
